apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: trust-manager
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "2"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: homelab
  source:
    chart: trust-manager
    repoURL: https://charts.jetstack.io
    # Épinglé sur la série 0.x : "*" suivrait aveuglément tout futur major
    # (supply chain) ; Renovate propose les bumps.
    targetRevision: "0.*"
    helm:
      releaseName: trust-manager
  destination:
    server: "https://kubernetes.default.svc"
    namespace: cert-manager
  
  # Sync policy
  syncPolicy:
    syncOptions:
      - CreateNamespace=true  
    automated: 
      prune: true
      selfHeal: true